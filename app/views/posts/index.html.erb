<h1>Posts</h1>

<% @posts.each do |post| %>
  <h1> <%= dom_id(post) %>
    <%= link_to post.title, post %>
  </h1><br>
  <p>
    <%= truncate(post.content, length:65) %>
  </p><br>
  <p>
    Submited <%= time_ago_in_words(post.created_at) %>.
  </p>
<% end %>
