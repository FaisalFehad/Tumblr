<%= form_for([@post, @post.comments.build]) do |f| %>
  <p>
    <%= f.label :name, "User name" %>
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.label :content, "Comment Content" %>
    <%= f.text_area :content %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>
