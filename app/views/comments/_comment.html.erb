<p><%= comment.name %></p><br>
<p><%= comment.content %></p>
<p><%= time_ago_in_words(comment.created_at) %> Ago</p>

<p> <%= link_to 'Delete Note', [comment.post, comment], method: :delete,
    data: {confirm: 'Are you sure?'} %>
</p>
